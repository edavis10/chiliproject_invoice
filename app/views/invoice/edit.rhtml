<h1>Editing Invoice</h1>

<% form_for(:invoice, @invoice, :url => { :action => 'update', :id => @project}) do |form| %>

<%= form.hidden_field 'id' -%>

<%= render(:partial => 'form', :object => form) %>

<p><%= form.submit "Update" %></p>

<% end %>
